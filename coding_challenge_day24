class Solution {
  public:
    int binaryToDecimal(string &b) {
        // Code here.
        int n=b.size();
        int sum=0;
        for(int i=n-1,j=0;i>=0;i--,j++){
            int r=b[i]-'0';
            sum+=r*pow(2,j);
        }
        return sum;
    }
};
