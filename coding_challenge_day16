class Solution {
public:
    int f(int n ,vector<int> &dp){
        if(n==0) return 0;
        else if(n==1) return 1;
        if(dp[n]!=-1) return dp[n];
        return dp[n]=fib(n-1)+fib(n-2);
    }
    int fib(int n) {
        if(n==0) return 0;
        if(n==1) return 1;
     
        int prev1=0;
        int prev=1;
        for(int i=2;i<=n;i++){
            int curr=prev+prev1;
            prev1=prev;
            prev=curr;
        }
        return prev;
    }
};
